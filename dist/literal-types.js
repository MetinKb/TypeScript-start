"use strict";
const num = 2.5; // This will always be equal to 2.5, not a type of number. Because const value does not change
let num2 = 2.5; // This will equal to type of number
// const result: "number" | "string" = "test"  // We can't assign another content except "number" and "string"
const result2 = "number";
